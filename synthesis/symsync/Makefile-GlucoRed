BASE      = glucose
EXEC      = symsync
DEPDIR    = $(BASE)/mtl $(BASE)/utils $(BASE)/core solver-reducer
CFLAGS    = -I$(MROOT)/$(BASE) -DGLUCORED
LFLAGS    = -pthread
OBJEXT    = o

include $(MROOT)/$(BASE)/mtl/template.mk
